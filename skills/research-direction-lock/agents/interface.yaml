interface:
  display_name: 方向锁定
  short_description: 方向锁定
  default_prompt: Use $research-direction-lock for the task described by the user.
compatibility:
  canonical_format: agent-skills
  adapter_targets:
  - generic
  activation:
    mode: model
  execution:
    context: inline
    shell: powershell
  trust:
    source_tier: local
    remote_inline_execution: forbid
    remote_metadata_policy: allow-metadata-only
  degradation:
    generic: neutral-source
