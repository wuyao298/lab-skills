interface:
  display_name: 领域建库与报告
  short_description: 领域建库与报告
  default_prompt: Use $research-field-report for the task described by the user.
compatibility:
  canonical_format: agent-skills
  adapter_targets:
  - generic
  activation:
    mode: model
  execution:
    context: inline
    shell: powershell
  trust:
    source_tier: local
    remote_inline_execution: forbid
    remote_metadata_policy: allow-metadata-only
  degradation:
    generic: neutral-source
