# 检索式迭代优化提示词（段5.2 Step 3，自动执行）

> 来源：incubation HTML 第1集提示词2「检索式迭代优化」，按段5.2 约束改写：迭代只调语义群/变体/排除词、不加 year/doctype；两个触发场景（判门 FAIL 相关性优化 / 规模门出窗收窄或放宽），全自动执行无人工确认。

## 场景 A：判门 FAIL（相关性优化）

这是我用你设计的检索式搜到的前 20 篇文章（按时间倒序），但很多不相关。请分析为什么这些不相关的论文会被检索出来，并基于这些错误，帮我优化检索式。

## 场景 B：规模门出窗（收窄或放宽）

判门 PASS 但 totalResults = {TOTAL}。规模窗口 = **50 < totalResults ≤ 1000**：
- **> 1000**：检索式过宽，请收窄（删除贡献量最大的宽泛分支/冗余变体、加强排除词或把泛化兜底短语改为 W/3 邻近限定）；目标 500-1000
- **≤ 50**：检索式过窄，请放宽（补同义词/近义词/子类型/拼写变体、删过严的限定）

## 当前检索式

```
{QUERY}
```

## 研究方向

- 方向契约摘要：{DIRECTION_SUMMARY}
- 该组合的子问题标签：{LABEL}

## 前 20 篇（标题 + 摘要，场景 A 用）

{ROUND_N_RAW_JSON 中前 20 篇的标题与摘要，逐篇列出}

## 任务

1. 逐篇判读不相关论文的进入原因（场景 A）或分析规模来源分支（场景 B）：命中了哪个概念片段、哪个词过宽、哪个词缺失
2. 给出 1-3 条具体修改（只允许：调语义群、调命名变体、调排除词；**禁止**加 year/doctype 限制、禁止换检索源）
3. 输出完整优化后的检索式（必须可直接运行，禁止占位符）

## 输出格式

```markdown
# 检索式迭代建议（第 N 轮，场景 A/B）

## 不相关原因分析 / 规模压缩分析
| 论文/分支 | 进入原因 | 修改方向 |
|---|---|---|

## 修改清单
1. ...

## 优化后检索式
TITLE-ABS-KEY(...)
```
